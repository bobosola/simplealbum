/target
.DS_Store
album.db*
# The live config holds the admin key; only album.example.toml is committed.
album.local.toml
prompts

# Local convenience script with machine-specific paths (mirrors static/ into
# the site repo). Nothing here is needed to build or run the album.
/copy_static.sh